var expect = require('chai').expect;
var chai = require('chai');

describe('Teste de vonfigurações da App', () => {

    before(() => {
    });

    after(() => {
    });

    it("Deve verificar se o teste unitário esta ok", done => {
        done();
    });

});